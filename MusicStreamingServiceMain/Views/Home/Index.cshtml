@model MusicStreamingService.Controllers.HomeViewModel
@{
    ViewData["Title"] = "Музыкальный стриминговый сервис";
}

<div class="hero-section">
    <div class="container">
        <h1 class="hero-title">Слушайте музыку, которая вам нравится</h1>
        <p class="hero-subtitle">Миллионы треков, персональные рекомендации и удобные плейлисты</p>
        <div class="hero-actions">
            <a href="#" class="btn btn-primary btn-lg">Начать слушать бесплатно</a>
            <a href="#" class="btn btn-outline btn-lg">Узнать о подписке</a>
        </div>
    </div>
</div>

<div class="container main-content">
    <!-- Поиск -->
    <div class="search-section">
        <form asp-controller="Search" asp-action="Index" method="get" class="search-form">
            <div class="search-input-group">
                <input type="text" name="query" class="search-input"
                       placeholder="Исполнитель, трек или альбом"
                       required
                       value="@ViewBag.SearchQuery" />
                <button type="submit" class="search-button">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </form>
    </div>

    <!-- Популярные треки -->
    <section class="section">
        <h2 class="section-title">Популярные треки</h2>
        <div class="tracks-grid">
            @foreach (var track in Model.PopularTracks)
            {
                <div class="track-card" data-track-id="@track.Id">
                    <div class="track-image">
                        <img src="https://via.placeholder.com/150" alt="@track.Title" />
                        <button class="play-button" onclick="playTrack(@track.Id, '@track.Title', '@track.Artist?.Name', @track.Duration)">
                            <i class="fas fa-play"></i>
                        </button>
                    </div>
                    <div class="track-info">
                        <h3 class="track-title">@track.Title</h3>
                        <p class="track-artist">@track.Artist?.Name</p>
                        <p class="track-album">@track.Album?.Title</p>
                    </div>
                    <div class="track-actions">
                        <span class="track-duration">@TimeSpan.FromSeconds(track.Duration).ToString(@"mm\:ss")</span>

                        <!-- Кнопка добавления в плейлист -->
                        <button class="add-to-playlist-btn"
                                data-track-id="@track.Id"
                                title="Добавить в плейлист">
                            <i class="fas fa-plus"></i>
                        </button>

                        <button class="like-button" onclick="likeTrack(@track.Id)">
                            <i class="far fa-heart"></i>
                        </button>
                    </div>
                </div>
            }
        </div>
    </section>

    <!-- Новые релизы -->
    <section class="section">
        <h2 class="section-title">Новые релизы</h2>
        <div class="albums-grid">
            @foreach (var album in Model.NewReleases)
            {
                <div class="album-card">
                    <div class="album-image">
                        <img src="https://via.placeholder.com/200" alt="@album.Title" />
                        <button class="play-button">
                            <i class="fas fa-play"></i>
                        </button>
                    </div>
                    <div class="album-info">
                        <h3 class="album-title">@album.Title</h3>
                        <p class="album-artist">@album.Artist?.Name</p>
                        <p class="album-year">@album.ReleaseDate?.Year</p>
                    </div>
                </div>
            }
        </div>
    </section>

    <!-- Избранные исполнители -->
    <section class="section">
        <h2 class="section-title">Избранные исполнители</h2>
        <div class="artists-grid">
            @foreach (var artist in Model.FeaturedArtists)
            {
                <div class="artist-card">
                    <div class="artist-image">
                        <img src="https://via.placeholder.com/150" alt="@artist.Name" />
                    </div>
                    <div class="artist-info">
                        <h3 class="artist-name">@artist.Name</h3>
                        <p class="artist-description">@(artist.Description?.Length > 100 ? artist.Description.Substring(0, 100) + "..." : artist.Description)</p>
                    </div>
                </div>
            }
        </div>
    </section>
</div>

<!-- Музыкальный плеер -->
<div class="music-player" id="musicPlayer" style="display: none;">
    <div class="player-controls">
        <button class="control-btn" onclick="previousTrack()">
            <i class="fas fa-step-backward"></i>
        </button>
        <button class="control-btn play-btn" onclick="togglePlay()">
            <i class="fas fa-play" id="playIcon"></i>
        </button>
        <button class="control-btn" onclick="nextTrack()">
            <i class="fas fa-step-forward"></i>
        </button>
    </div>

    <div class="player-info">
        <div class="track-now-playing">
            <span class="track-name" id="currentTrackName">Выберите трек</span>
            <span class="artist-name" id="currentArtistName">для воспроизведения</span>
        </div>
    </div>

    <div class="player-progress">
        <div class="progress-bar" onclick="seek(event)">
            <div class="progress" id="progressBar"></div>
        </div>
        <div class="time-display">
            <span class="current-time" id="currentTime">0:00</span>
            <span class="total-time" id="totalTime">0:00</span>
        </div>
    </div>

    <div class="player-options">
        <button class="option-btn" onclick="addCurrentTrackToPlaylist()" title="Добавить в плейлист" id="addToPlaylistBtn">
            <i class="fas fa-plus"></i>
        </button>

        <button class="option-btn" onclick="toggleMute()">
            <i class="fas fa-volume-up" id="volumeIcon"></i>
        </button>
        <input type="range" min="0" max="100" value="50" class="volume-slider" id="volumeSlider" onchange="setVolume(this.value)">
    </div>


<style>
    .add-to-playlist-btn {
        background: none;
        border: none;
        color: #666;
        cursor: pointer;
        padding: 5px;
        transition: color 0.2s;
    }

        .add-to-playlist-btn:hover {
            color: #007bff;
        }

    .track-actions {
        display: flex;
        align-items: center;
        gap: 10px;
    }
</style>

@section Scripts {
    <script>
        // Глобальные переменные плеера
        let currentAudio = null;
        let currentTrackId = null;
        let isPlaying = false;
        let currentTrackIndex = 0;
        let tracks = [];
        let currentPlayingTrackId = null;

        // Инициализация треков из страницы
        document.addEventListener('DOMContentLoaded', function() {
            const trackCards = document.querySelectorAll('.track-card');
            tracks = Array.from(trackCards).map(card => ({
                id: card.dataset.trackId,
                title: card.querySelector('.track-title').textContent,
                artist: card.querySelector('.track-artist').textContent,
                duration: parseInt(card.querySelector('.track-duration').textContent.replace(':', ''))
            }));
        });

        // Воспроизведение трека
        function playTrack(trackId, title, artist, duration) {
            
            // currentPlayingTrackId = trackId;
            
            // updateAddToPlaylistButton(trackId);
            
            // Останавливаем текущее воспроизведение
            if (currentAudio) {
                currentAudio.pause();
            }

            // Показываем плеер
            document.getElementById('musicPlayer').style.display = 'flex';

            // Обновляем информацию о треке
            document.getElementById('currentTrackName').textContent = title;
            document.getElementById('currentArtistName').textContent = artist;
            document.getElementById('totalTime').textContent = formatTime(duration);

            // Создаем новый аудио элемент
            currentAudio = new Audio(`/Tracks/Stream/${trackId}`);
            currentTrackId = trackId;

            // Настройка событий аудио
            currentAudio.addEventListener('loadedmetadata', function() {
                document.getElementById('totalTime').textContent = formatTime(currentAudio.duration);
            });

            currentAudio.addEventListener('timeupdate', function() {
                updateProgress();
            });

            currentAudio.addEventListener('ended', function() {
                nextTrack();
            });

            // Начинаем воспроизведение
            currentAudio.play();
            isPlaying = true;
            updatePlayButton();

            // Логируем прослушивание
            fetch(`/Tracks/RecordPlay/${trackId}`, { method: 'POST' });
        }

        // Управление воспроизведением
        function togglePlay() {
            if (!currentAudio) return;

            if (isPlaying) {
                currentAudio.pause();
            } else {
                currentAudio.play();
            }
            isPlaying = !isPlaying;
            updatePlayButton();
        }

        function updatePlayButton() {
            const playIcon = document.getElementById('playIcon');
            if (isPlaying) {
                playIcon.className = 'fas fa-pause';
            } else {
                playIcon.className = 'fas fa-play';
            }
        }

        // Обновление прогресса
        function updateProgress() {
            if (!currentAudio) return;

            const progress = (currentAudio.currentTime / currentAudio.duration) * 100;
            document.getElementById('progressBar').style.width = progress + '%';
            document.getElementById('currentTime').textContent = formatTime(currentAudio.currentTime);
        }

        // Перемотка
        function seek(event) {
            if (!currentAudio) return;

            const progressBar = event.currentTarget;
            const rect = progressBar.getBoundingClientRect();
            const percent = (event.clientX - rect.left) / rect.width;

            currentAudio.currentTime = percent * currentAudio.duration;
        }

        // Управление громкостью
        function setVolume(value) {
            if (currentAudio) {
                currentAudio.volume = value / 100;
            }
        }

        function toggleMute() {
            if (!currentAudio) return;

            currentAudio.muted = !currentAudio.muted;
            const volumeIcon = document.getElementById('volumeIcon');
            volumeIcon.className = currentAudio.muted ? 'fas fa-volume-mute' : 'fas fa-volume-up';
        }

        // Навигация по трекам
        function nextTrack() {
            if (tracks.length === 0) return;

            currentTrackIndex = (currentTrackIndex + 1) % tracks.length;
            const track = tracks[currentTrackIndex];
            playTrack(track.id, track.title, track.artist, track.duration);
        }

        function previousTrack() {
            if (tracks.length === 0) return;

            currentTrackIndex = (currentTrackIndex - 1 + tracks.length) % tracks.length;
            const track = tracks[currentTrackIndex];
            playTrack(track.id, track.title, track.artist, track.duration);
        }

        // Вспомогательные функции
        function formatTime(seconds) {
            const mins = Math.floor(seconds / 60);
            const secs = Math.floor(seconds % 60);
            return `${mins}:${secs.toString().padStart(2, '0')}`;
        }

        // Лайк трека
        function likeTrack(trackId) {
            // В будущем можно добавить функционал лайков
            console.log('Liked track:', trackId);
        }
            // Функция добавления трека в плейлист
        function addToPlaylist(trackId) {
            // Проверяем авторизацию
            if (!isUserAuthenticated()) {
                alert('Для добавления треков в плейлисты необходимо войти в систему');
                window.location.href = '/Account/Login';
                return;
            }

            // Открываем модальное окно
            if (typeof openAddToPlaylistModal === 'function') {
                openAddToPlaylistModal(trackId);
            }
        }

        // Проверка авторизации пользователя
        function isUserAuthenticated() {
            // Простая проверка наличия куки или другого признака авторизации
            return document.cookie.includes('.AspNetCore.Cookies');
        }

            // Добавление текущего трека в плейлист
        function addCurrentTrackToPlaylist() {
            if (!currentTrackId) {
                alert('Сначала выберите трек для воспроизведения');
                return;
            }

            addToPlaylist(currentTrackId);
        }

                // Обновление кнопки добавления в плейлист
            function updateAddToPlaylistButton(trackId) {
                const addBtn = document.getElementById('addToPlaylistBtn');
                if (addBtn) {
                    addBtn.dataset.trackId = trackId;
                    addBtn.title = `Добавить "${title}" в плейлист`;
                    addBtn.style.display = 'flex';
                }
            }

            // Добавление текущего трека в плейлист
            function addCurrentTrackToPlaylist() {
                if (!currentPlayingTrackId) {
                    showToast('Сначала выберите трек для воспроизведения', 'error');
                    return;
                }

                // Используем глобальную функцию из playlists.js
                if (typeof window.addToPlaylist === 'function') {
                    window.addToPlaylist(currentPlayingTrackId);
                } else {
                    // Если playlists.js еще не загружен, открываем модальное окно напрямую
                    openAddToPlaylistModal(currentPlayingTrackId);
                }
            }

            // Функция открытия модального окна (если playlists.js не загружен)
            function openAddToPlaylistModal(trackId) {
                // Проверяем авторизацию
                if (!isUserAuthenticated()) {
                    alert('Для добавления треков в плейлисты необходимо войти в систему');
                    window.location.href = '/Account/Login';
                    return;
                }

                // Открываем модальное окно
                fetch(`/Playlists/AddToPlaylistModal?trackId=${trackId}`)
                    .then(response => response.text())
                    .then(html => {
                        // Создаем модальное окно
                        const modalContainer = document.createElement('div');
                        modalContainer.innerHTML = html;
                        document.body.appendChild(modalContainer.firstElementChild);

                        // Инициализируем и показываем модальное окно
                        const modal = new bootstrap.Modal(document.getElementById('addToPlaylistModal'));
                        modal.show();
                    });
            }
    </script>
}